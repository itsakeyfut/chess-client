use bevy::prelude::*;

pub struct InputPlugin;

impl Plugin for InputPlugin {
    fn build(&self, app: &mut App) {
        // TODO: 入力システムの実装
        info!("Input plugin loaded (placeholder)")
    }
}