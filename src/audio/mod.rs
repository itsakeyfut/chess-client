use bevy::prelude::*;

pub struct AudioPlugin;

impl Plugin for AudioPlugin {
    fn build(&self, app: &mut App) {
        // TODO: オーディオシステムの実装
        info!("Audio plugin loaded (placeholder)");
    }
}